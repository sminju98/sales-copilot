# 코파일럿 협업 프로토콜 v1

코파일럿 시리즈가 한 사용자의 머신에서 **부딪히지 않고 협력**하기 위한 규격이다.
이 문서를 따르는 플러그인은 서로의 존재를 몰라도 같은 조직으로 동작한다.

---

## 0. 조직

| 이름 | 직무 | 순위 |
|---|---|---|
| `business-copilot` | **대표** | 0 |
| `marketing-copilot` | 마케팅 임원 | 1 |
| `sales-copilot` | 영업 임원 | 2 |
| `pm-copilot` | 기획 임원 | 3 |

- **대표는 지휘하고, 임원은 자기 직무 안에서 자율**이다. 임원의 승인 게이트·예산 상한·발송 한도는 대표가 우회할 수 없다 — 지휘는 **무엇을 먼저 할지**를 정하는 것이지 **한계를 푸는 것**이 아니다.
- 대표가 설치돼 있지 않으면 **직무 순위가 가장 앞선 임원이 대행**한다. 지휘자 없는 상태는 없다.
- 4형제가 아닌 플러그인도 이 프로토콜을 따르면 "협력 모듈"(순위 9)로 참여한다.

---

## 1. 왜 프로토콜이 필요한가

코파일럿은 각자 `SessionStart` 훅을 건다. **훅은 병렬로 실행되고 순서가 보장되지 않는다.** 그대로 두면 세션을 열 때마다 넷이 동시에 자기 브리핑을 쏟아내고, 사용자는 누구 말을 들어야 할지 모른다.

"대표가 먼저 말하게 하라"는 순서 규칙으로는 못 푼다 — 순서를 정할 수단이 없기 때문이다. 그래서 **발화와 수집을 분리**한다.

```
임원  ──[안건 게시]──▶  공용 버스  ──[수집]──▶  대표  ──[1회 발화]──▶  사용자
(침묵)                                          (지휘)
```

---

## 2. 공용 버스

### 위치
```
~/.copilot-org/                       # COPILOT_ORG_HOME 로 재정의 가능
├── roster.json                       # 명부 — 누가 설치돼 있나
└── sessions/<session_id>.json        # 세션별 안건
```

### 명부 `roster.json`
```json
{
  "marketing-copilot": {
    "role": "마케팅 임원",
    "home": "/절대/경로/플러그인루트",
    "last_seen": 1785900000.0
  }
}
```
- 세션을 넘어 남는다. 대표가 **누구를 기다려야 하는지** 아는 근거다.
- `last_seen`이 14일을 넘으면 명부에서 내린다. 없는 사람을 기다리다 매 세션이 느려지는 것을 막는다.

### 안건 `sessions/<session_id>.json`
```json
{
  "marketing-copilot": {
    "role": "마케팅 임원",
    "items": ["성과 미기록 3건 — 측정 없는 게시 금지", "…"],
    "ts": 1785900000.0
  }
}
```
- `items`는 **사용자에게 보여줄 완성문이 아니라 에이전트에게 주는 지시문**이다. 언어 규칙(6절)이 적용된다.
- 최근 40개 세션만 남기고 정리한다.

---

## 3. 절차

모든 참여 플러그인의 `SessionStart` 훅은 정확히 이 순서를 따른다.

1. **등록** — `org.register()`. **조기 반환보다 먼저** 한다. 설정이 없어 바로 빠져나가는 경우에도 명부에는 남아야 대표가 기다릴 대상을 안다.
2. **게시** — 자기 안건을 `org.post(session_id, items)`로 버스에 올린다. 안건이 없으면 빈 배열로 올린다(도착 사실 자체가 신호다).
3. **판정** — `org.is_chair()`
   - **거짓(임원)**: 아무것도 출력하지 않고 종료한다.
   - **참(대표)**: 4로 간다.
4. **수집** — `org.collect(session_id)`. 명부에 오른 전원이 도착하면 즉시, 아니면 **최대 2.5초** 기다리고 있는 것만 쓴다. 훅이 세션을 붙잡으면 안 된다.
5. **발화** — `org.render()`로 직무 순서대로 묶어 **한 번만** 출력한다.

### 실패는 조용히 예전 동작으로
조직 모듈을 못 불러오거나 버스 접근에 실패하면 **자기 브리핑을 혼자 출력한다.** 조율 실패가 브리핑 자체를 없애면 안 된다. 부딪히는 것보다 나쁜 건 아무 말도 안 하는 것이다.

---

## 4. 세션 식별자

```
훅 stdin 의 session_id  →  CLAUDE_CODE_SESSION_ID  →  CLAUDE_SESSION_ID  →  "nosession"
```
앞의 것이 우선한다. 파일명으로 쓰므로 영숫자·`-`·`_`만 남기고 64자로 자른다.

---

## 5. 상태 격리 — 서로의 원장을 건드리지 않는다

| 종류 | 위치 | 규칙 |
|---|---|---|
| 자기 상태 | `~/.<플러그인이름>/` | **쓰기는 자기 것만.** 남의 홈에 쓰지 않는다 |
| 남의 맥락 | `~/.<형제>/context/…` | **읽기만.** 참고 자료로 쓴다 |
| 조직 버스 | `~/.copilot-org/` | 읽고 쓴다. 단 자기 키(`<플러그인이름>`)만 갱신한다 |
| 인계 | `~/.<받는쪽>/handoffs/inbox/` | 계약서 형식으로만. 상대 원장을 직접 고치지 않는다 |

**동시 쓰기가 있는 파일은 반드시 잠근다.** 원장(JSONL)은 `read → 수정 → 전체 재작성` 구조라, 잠그지 않으면 남의 쓰기가 통째로 사라지고 채번이 겹쳐 중복 id가 생긴다. 중복 id는 오류 없이 퍼지고 성과 귀속을 조용히 깨뜨린다.

```python
with file_lock(path):        # common.py
    rows = read_all(...)
    ...
    _rewrite(path, rows)     # 임시파일에 fsync 후 os.replace
```
임시 파일 이름에는 **pid를 붙인다.** 고정 이름이면 두 프로세스가 같은 tmp를 밟는다.

---

## 6. 언어

**스킬 문서가 한국어인 것과 사용자가 쓰는 언어는 별개다.** 문서는 에이전트에게 주는 지시문이지 사용자에게 보여줄 글이 아니다.

```
config language 가 auto 아님  →  그 언어
auto(기본) 또는 없음          →  사용자가 방금 쓴 언어
판단 불가                     →  한국어
```

- 버스에 올리는 `items`, 스크립트 출력, 훅이 주입한 문장 — **전부 지시문이다.** 그대로 복사하지 말고 사용자 언어로 다시 말한다.
- 고유명사는 번역하지 않는다(네이버·GA4·ROAS 등).
- **대화 언어와 산출물 언어는 다르다.** 영어로 대화해도 한국 시장에 낼 글은 한국어로 쓴다.

---

## 7. 스킬 이름 충돌

4형제가 같은 이름의 스킬을 갖는 경우가 12건 있다(`setup` `method` `update` `help` `role` `context` `handoff` `routine` `today` `metrics` `opportunity` `weekly-review`). Claude Code는 `플러그인:스킬`로 네임스페이스를 붙여 충돌을 막지만, **평면 구조인 런타임에서는 나중 것이 앞을 가린다.**

- 이 프로토콜을 따르는 플러그인은 **평면 배치 시 `<플러그인접두어>-<스킬>` 로 이름을 바꿔** 배포한다.
- 사용자가 직무를 명시하지 않고 부르면(예: "설정"), **대표가 직무를 판정해 라우팅**한다.

---

## 8. 실행 경로 — 환경변수에 기대지 않는다

`$CLAUDE_PLUGIN_ROOT`는 **훅·커맨드 정의에서만 치환되고 에이전트가 실제로 명령을 실행하는 셸에는 내려오지 않는다.** 그 변수를 쓴 명령줄은 조용히 실패한다.

```
bin/<플러그인이름>          # 자기 위치로 뿌리를 찾는 디스패처
```
- Claude Code는 `bin/`을 PATH에 자동으로 넣는다 → 이름만 부르면 된다.
- 다른 런타임에서는 이 파일을 직접 부르거나 `bin/`을 PATH에 넣는다.
- 훅 정의도 이 디스패처를 경유해 **진입점을 하나로** 모은다.

---

## 9. 참여하려면

1. `scripts/org.py`를 그대로 복사한다(4형제에서 **바이트까지 동일**하다. 자기 이름은 매니페스트에서 읽는다).
2. `ROLES`에 자기 이름·직무·순위를 추가한다. 없으면 "협력 모듈"(9)로 참여한다.
3. `SessionStart` 훅에서 3절 절차를 따른다.
4. 5절 상태 격리와 6절 언어 규칙을 지킨다.

```bash
<플러그인이름> org status     # 나는 누구이고 명부에 누가 있나
```

---

---

## 11. 다른 에이전트로 내보내기

스킬은 **공통 경로 하나**로 나간다.

```
~/.agents/skills/<접두어>-<스킬>/SKILL.md
```

이 경로를 읽는 에이전트 — **GitHub Copilot · Codex CLI · Cursor · Zed · Antigravity.** 한 번 내보내면 다섯이 같이 본다. 에이전트마다 따로 복사하면 사본이 다섯 벌이 되고 곧 갈라진다.

**`~/.claude/` 에는 내보내지 않는다.** Copilot·Cursor·Cline 이 `~/.claude/skills` 와 `~/.claude/settings.json` 도 읽는데 그건 Claude Code 자기 파일이다. 거기 내보내면 Claude Code 에서 플러그인 스킬과 복사본이 둘 다 잡히고, 훅은 플러그인 정의와 settings 정의가 **이중으로 발동**한다. `~/.agents/` 는 Claude Code 가 안 읽으므로 충돌 면이 0이다.

```bash
<플러그인이름> port                    # 무엇이 생길지 보기만
<플러그인이름> port --apply            # 내보내기
<플러그인이름> port --status           # 지금 무엇이 나가 있나
<플러그인이름> port --remove           # 걷어내기
```

정본(`skills/`)은 건드리지 않는다. 산출물은 언제든 다시 만들 수 있다.

### 내보낼 때 반드시 고치는 것
| 항목 | 왜 |
|---|---|
| 스킬 이름 → `<접두어>-<이름>` | 평면 목록이라 4형제의 겹치는 12개가 서로를 가린다 |
| `[[스킬]]` → `[[<접두어>-스킬]]` | 안 맞추면 1,000건 넘는 상호참조가 허공을 가리킨다 |
| `$(플러그인 --root)` → 절대 경로 | `bin/` 을 PATH 에 넣어 주는 건 Claude Code 뿐이다 |
| 폴더 밖 `skills/x/y.md` → 절대 경로 | 평면 배치에서는 도달 경로가 없다 |
| 부속 파일 동반 복사 | `communities-kr.md` 같은 파일이 빠지면 스킬이 반쪽이 된다 |

### 스키마 상한 (초과하면 스킬이 아예 안 잡힌다)
- `name` ≤ 64자, 소문자·숫자·하이픈만
- `description` ≤ 1024자

### 훅은 공통 경로가 없다
에이전트마다 설정 위치와 문법이 다르므로 타깃별로 쓴다. 단 **가리키는 디스패처는 하나**다.

| 타깃 | 위치 |
|---|---|
| GitHub Copilot | `~/.copilot/hooks/<플러그인>.json` |
| Cursor | `~/.cursor/hooks.json` |
| Codex CLI | `~/.codex/hooks.json` |

`~/.codex/config.toml` 에 `[hooks]` 를 인라인으로 넣지 않는다 — 한 레이어에 JSON 과 인라인이 둘 다 있으면 Codex 가 **둘 다 로드하고 경고**를 낸다. 게다가 그 파일은 ChatGPT 앱이 쓰는 살아있는 설정이다.

여러 코파일럿이 같은 훅 파일을 공유하므로 **덮어쓰지 않고 병합**한다. 자기 디스패처를 가리키는 항목만 갈아 끼우고 남의 것은 그대로 둔다. `--remove` 도 자기 것만 걷어낸다.

### 카탈로그 예산 — 실측값

Codex 는 스킬 목록에 컨텍스트의 **2% 만** 쓴다. 넘으면 description 이 잘리는데, 자동 발동이 description 으로 이뤄지므로 **스킬이 아예 안 불린다.**

`codex debug prompt-input` 으로 직접 잰 값 (codex-cli 0.145.0-alpha.30):

| 심은 스킬 수 | 내장 스킬 description | 판정 |
|---|---|---|
| 40개 | 570자 (원본 그대로) | ✅ 온전 |
| 80개 | 84자 | ⚠️ 축약 |
| 164개 | 32자 | ❌ 뭉개짐 |

**예산은 전 소스 공유다.** 넘기면 우리 것뿐 아니라 그 런타임의 내장 스킬(`imagegen` 570→30자)까지 함께 뭉갠다. 남의 도구를 망가뜨리는 셈이라, 개수를 지키는 것은 성능 문제이자 예의 문제다.

**Codex 를 쓸 거면 40개 안쪽으로 맞춘다.** Copilot 에는 이 제한이 없어 전부 넣어도 된다.

```bash
<플러그인이름> port --apply --max 20     # 백본 + 많이 참조되는 순으로 20개만
```
무엇이 빠졌는지 **반드시 출력한다.** 조용히 자르면 "다 넣었다"로 읽힌다.

⚠️ **Copilot 은 `matcher` 를 무시하고 모든 툴에서 훅을 돌린다.** PostToolUse 훅은 자기가 입력을 보고 걸러야 한다 — 파일 경로가 없으면 조용히 끝내는 식으로.

## 10. 버전

| | |
|---|---|
| 프로토콜 | v1 |
| 최초 | 2026-08-05 |
| 규격 소유 | 이미지팩토리 송민주 · MIT |

호환 규칙 — `roster.json`·`sessions/*.json`에 **모르는 키가 있으면 무시하고 통과**한다. 새 필드 추가는 하위호환을 깨지 않는다.
